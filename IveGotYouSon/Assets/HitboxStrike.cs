using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HitboxStrike : MonoBehaviour
{
    public PlayerMovement playerMovement;

    private void Start()
    {
        
    }

    private void Update()
    {
        //transform.position = playerMovement.transform.position + new Vector3(0.5f + playerMovement.moveX, 0.5f + playerMovement.moveY, 0);
    }
}
